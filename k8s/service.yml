apiVersion: v1
kind: Service
metadata:
  name: taskflow-service
  namespace: taskflow
spec:
  selector:
    app: taskflow
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000
  type: NodePort